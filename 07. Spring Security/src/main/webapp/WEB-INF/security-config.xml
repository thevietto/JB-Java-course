<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <security:http auto-config="true" use-expressions="true" access-denied-page="/forbidden">
        <security:intercept-url pattern="/panel/**" access="hasAnyRole('ROLE_USER', 'ROLE_ADMIN')"/>
        <security:form-login
                login-page="/login"
                login-processing-url="/login/process"
                password-parameter="password"
                username-parameter="login"
                authentication-failure-url="/login?error=true"/>
        <security:logout logout-url="/logout"/>
        <!--<security:remember-me key="x" token-validity-seconds="10" remember-me-parameter="x"/>-->
    </security:http>

    <security:authentication-manager>
        <security:authentication-provider>
            <!--<security:user-service>-->
            <!--<security:user name="admin" authorities="ROLE_USER" password="admin"/>-->
            <!--<security:user name="admin1" authorities="ROLE_NOUSER" password="admin"/>-->
            <!--</security:user-service>-->

            <!--<security:password-encoder hash="md5">-->
                <!--<security:salt-source user-property="THIS" ref="ORTHIS" />-->
            <!--</security:password-encoder>-->

            <security:password-encoder ref="encoder"/>

            <security:jdbc-user-service data-source-ref="dataSource"
                                        users-by-username-query="select username, password, true from users where username =?"
                                        authorities-by-username-query="SELECT username, 'ROLE_USER' from users where username=?"/>

        </security:authentication-provider>
    </security:authentication-manager>

    <bean id="encoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

</beans>